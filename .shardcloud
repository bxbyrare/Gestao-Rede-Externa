DISPLAY_NAME=Gestao Rede Externa
SUBDOMAIN=gestaoredeexterna
LANGUAGE=python
MEMORY=1024
VERSION=recommended
CUSTOM_COMMAND=pip install -r requirements.txt && cd frontend && npm install && npm run build && cd .. && python app.py
